#!/usr/bin/env bash

set -euo pipefail

PROJECT="$(dirname "$(dirname "$(realpath "${0}")")")"

source "${PROJECT}/bin/env"

VERSION="$(cat "${PROJECT}/version")"
FILE="${PROJECT}/artifacts/arktika-icebreaker-${VERSION}-py3-none-any.whl"

twine upload --repository testpypi "${FILE}"
