#!/usr/bin/env bash

set -euo pipefail

REPO="$(dirname "$(dirname "$(realpath "$0")")")"

source "$REPO/bin/env"

VERSION="$(cat "$REPO/version")"
FILE="$REPO/artifacts/arktika-icebreaker-${VERSION}-py3-none-any.whl"

twine upload --repository testpypi "$FILE"
