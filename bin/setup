#!/usr/bin/env bash

set -euo pipefail

REPO="$(dirname "$(dirname "$(realpath "$0")")")"

"$REPO/bin/clean"
brew install python@3.12
python3.12 -m venv --copies --clear "$REPO/.env"
source "$REPO/bin/env"
python -m pip install --upgrade pip
python -m pip install --editable "${REPO}[dev]"
